{
  "name": "client-desktop-management",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "SET NODE_ENV=development&electron .",
    "start:mac": "NODE_ENV=development electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:mac": "electron-packager . Client-Desktop-Management --overwrite --out=dist/osx --platform=darwin --arch=x64",
    "build:win": "electron-packager . Client-Desktop-Management --overwrite --out=dist/win --platform=win32 --arch=x64"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^18.2.0"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.1.1",
    "axios": "^0.27.2",
    "electron-packager": "^15.5.1",
    "request": "^2.88.2"
  }
}
